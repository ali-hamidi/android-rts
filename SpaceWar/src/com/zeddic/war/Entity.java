package com.zeddic.war;

import com.zeddic.game.common.GameObject;

public class Entity extends GameObject {
  
  /*public Rectangle bounds;
  public float x;
  public float y;
  public Vector2d velocity = new Vector2d(0, 0);
  
  public Entity() {
    
  }
  
  public void update(int time) {
    float scale = (float) time / 200;
    x += velocity.x * scale;
    y += velocity.y * scale ;
  }
  
  public void draw(Graphics g) {
    
  }
  
  public void collide(Vector2d projection, Entity other) {

    if (projection.x != 0) {
      x += projection.x;
      velocity.x = 0;
    }
    
    if (projection.y != 0) {
      y += projection.y;
      velocity.y = 0;
    }
  }
  
  public float top() {
    return y - bounds.height / 2;
  }
  
  public float bottom() {
    return y + bounds.height / 2;
  }
  
  public float left() {
    return x - bounds.width / 2;
  }
  
  public float right() {
    return x + bounds.width / 2;
  }
  
  public void setTop(float top) {
    y = top + bounds.height / 2;
  }
  
  public void setLeft(float left) {
    x = left + bounds.width / 2;
  }
  
  public void setBottom(float bottom) {
    y = bottom - bounds.height / 2;
  }
  
  public void setRight(float right) {
    x = right - bounds.width / 2;
  } */
}
